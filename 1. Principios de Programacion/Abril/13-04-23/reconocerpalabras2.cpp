#include <iostream>
using namespace std;

int main()
{
    char c;
    char array[200];
    int i = 0;

    cout << "Introducir una frase \n";

    while ((c = getchar()) != EOF)
    {
        //empezar desde el incio a ver si econtramos papaya, ir cuadro por cuadro del indice del array, hay que calcular cuando hay que comenzar a  leer.
    }

    return 0;
}